"""IF_ELSE node — deterministic conditional routing (when: expressions)."""

from agent_compose.nodes.if_else.node import DEFAULT_HANDLE, Case, IfElseNode

__all__ = ["Case", "IfElseNode", "DEFAULT_HANDLE"]
