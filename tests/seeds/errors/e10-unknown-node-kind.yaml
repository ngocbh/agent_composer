# e10 — unknown node kind. COMPILE: "unknown kind 'llm'".
id: err-unknown-kind
name: err_unknown_kind
input:
  topic: str
nodes:
  note:
    kind: llm          # ERROR: not agent/code/model/tool/case/ref/map
    input:
      topic: ${input.topic}
    output: str
    prompt: "Rate ${topic}."
output: ${note.output}
