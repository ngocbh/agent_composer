# e20 — a CODE callable that raises. RUNTIME: the node fails -> the run fails.
id: err-code-raises
name: err_code_raises
input:
  topic: str
nodes:
  boom:
    kind: code
    input:
      topic: ${input.topic}
    output: str
    code: tests.seeds.fns:fail_always
output: ${boom.output}
