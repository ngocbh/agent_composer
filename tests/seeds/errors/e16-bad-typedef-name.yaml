# e16 — a typedef name shadows a typing constructor. COMPILE (read_typedefs).
id: err-bad-typedef-name
name: err_bad_typedef_name
typedefs:
  Optional: str          # ERROR: shadows the `Optional` typing constructor
input:
  topic: str
nodes:
  note:
    kind: agent
    input:
      topic: ${input.topic}
    output: str
    prompt: "Rate ${topic}."
output: ${note.output}
