# e12 — a kind-required field omitted. COMPILE: "missing required field 'code'".
id: err-missing-required-field
name: err_missing_required_field
input:
  topic: str
nodes:
  calc:
    kind: code         # ERROR: missing `code:`
    input:
      topic: ${input.topic}
    output: str
output: ${calc.output}
